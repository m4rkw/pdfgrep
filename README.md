# pdfgrep

````
usage:

pdfgrep.py -i [-c] [--ignore <path>] [--ignore <path] <path>   : index pdfs at path
-c will drop database entries for files at <path> no longer on disk or in ignored paths

pdfgrep.py <string | regex>                                    : grep pdfs
````
